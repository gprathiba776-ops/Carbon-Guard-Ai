# Deterministic Calculator API

The calculator is intentionally boring: it multiplies a verified quantity by a verified factor and converts kg CO2e to tCO2e.

It refuses to calculate unless `factor_status == VERIFIED`.
